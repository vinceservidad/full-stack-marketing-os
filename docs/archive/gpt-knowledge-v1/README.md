# Archived hand-maintained GPT summaries

These nine topic summaries preserve the previous `gpt-knowledge/` export layer.
They have been superseded by the generated pack in `gpt-knowledge/pack/` and
should not be uploaded alongside it. Their content is preserved for historical
review, not endorsed as current coverage or operating guidance.

The replacement is generated from current canonical skills, governance
contracts, and supporting Markdown libraries. Its manifest records exact source
coverage and hashes; export checks detect source drift and unassigned skills.
Those checks do not establish a GPT's actual retrieval or decision quality.

The optional `vince-style.md` and `humanizer-rules.md` remain in `gpt-knowledge/`
as hand-maintained voice guidance.
